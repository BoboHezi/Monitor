<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monitor"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >

        <!-- 欢迎界面   主界面 -->
        <activity android:name="com.example.monitor.WelcomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 功能界面 -->
        <activity android:name="com.example.monitor.MainActivity" >
        </activity>
        <!-- 登录界面 -->
        <activity android:name="com.example.monitor.LoginActivity" >
        </activity>
        <!-- 设置界面 -->
        <activity android:name="com.example.monitor.SetupActivity" >
        </activity>
        <!-- 注册界面 -->
        <activity android:name="com.example.monitor.RegisterActivity" >
        </activity>
        <!-- 修改密码界面 -->
        <activity android:name="com.example.monitor.ReviseActivity" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

</manifest>